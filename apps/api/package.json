{
  "name": "@freelancefinder/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format cjs --sourcemap --clean",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^4.26.2"
  },
  "devDependencies": {
    "@freelancefinder/config": "workspace:*",
    "@types/node": "^20.12.7",
    "tsup": "^8.0.1",
    "tsx": "^4.7.1",
    "vitest": "^1.5.0"
  }
}
